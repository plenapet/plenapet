/**
 * Dominio real de PlenaPet — todavía no está decidido definitivamente (ver
 * vault/Pendientes/Preguntas-abiertas.md). Mientras tanto se usa este
 * placeholder para poder generar sitemap/robots/metadata con URLs
 * absolutas. ACTUALIZAR en cuanto haya dominio confirmado, seteando
 * NEXT_PUBLIC_SITE_URL en el entorno de producción — no hace falta tocar
 * código.
 */
export const SITE_URL = process.env.NEXT_PUBLIC_SITE_URL || "https://plenapet.co";
